entity Webapp{


	entity Database(Webapplication, Actor, Filesystem: agent){
	symbols
		body{
				Webapplication -> Actor : evil_file;
				Actor -> Webapplication : ok_db;
		}
}
	entity Filesystem(Webapplication, Database, Actor: agent){

			%symbols
				%ok_fs : text;

			body{
				Webapplication -> Actor : evil_file;
				Actor -> Webapplication : ok_fs;
		}%body
	}%fs_entity
}
